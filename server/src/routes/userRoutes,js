const User = require('./models/userModel');

app.get('/api/users', async (req, res) => {
  try {
    const users = await User.find({}, 'name email'); // return name & email only
    res.json(users);
  } catch (err) {
    res.status(500).json({ message: 'Server error' });
  }
});
